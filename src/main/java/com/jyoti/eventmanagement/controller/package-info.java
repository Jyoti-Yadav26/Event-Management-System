/**
 * REST controllers — the entry point for HTTP requests.
 * <p>
 * Controllers should stay thin: validate input, call the service layer, return {@code ResponseEntity}.
 */
package com.jyoti.eventmanagement.controller;
