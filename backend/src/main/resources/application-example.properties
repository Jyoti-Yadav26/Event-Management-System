# =============================================================================
# Local configuration template
# =============================================================================
# Copy this file to application-local.properties in the same directory.
# application-local.properties is gitignored and must NOT be committed.
#
# Alternatively, set environment variables:
#   DB_PASSWORD, JWT_SECRET, DB_USERNAME (optional), DB_URL (optional)
# =============================================================================

# MySQL password
spring.datasource.password=your_mysql_password_here

# JWT signing key — minimum 32 characters
jwt.secret=${JWT_SECRET:mySuperSecretKeyForJwtAuthentication123456789}

